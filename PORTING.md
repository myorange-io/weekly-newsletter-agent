# 이식 가이드 — 일을 어디서 자를 것인가

에이전트를 몇 명 둘지는 취향이 아닙니다. **판단 기준이 갈리는 자리**에서 잘라야 합니다.
아래 네 가지는 오렌지레터를 매주 돌리면서 하나씩 깨진 뒤에 세운 규칙입니다.

---

## 규칙 1. 한 에이전트에 상충하는 목표를 주지 않는다

**수집가**의 목표는 "어떻게든 본문을 긁어온다"입니다.
**문지기**의 목표는 "쓸 수 없는 건 버린다"입니다.

이 둘을 한 명에게 맡기면 어떻게 될까요. 성공시키려는 압력이 이깁니다.
본문이 비어 있어도 "메타데이터라도 있으니 통과"시키고, 그 빈 내용이 LLM에 닿습니다.

> **실제로 벌어진 일**: 게시글이 없는 빈 게시판 페이지에서 메뉴·버튼 같은 UI 문자열만 추출됐는데
> 그게 본문으로 통과했습니다. LLM은 도메인 주소만 보고 **존재하지 않는 재단 이름과 사업명, 마감일을 지어냈습니다.**
> 그럴듯해서 아무도 눈치채지 못할 뻔했습니다.

목표가 반대인 둘은 반드시 다른 호출로 나눕니다. 문지기가 반려하는 건 실패가 아니라 **정상 경로**입니다.

> **우리도 완전히 지키지 못하고 있습니다.** 오렌지레터의 수집가는 자기 안에서 품질 판정을 일곱 번 합니다.
> 게다가 하한이 두 개입니다 — 수집을 계속할지 정하는 하한과 AI에 넣을지 정하는 하한.
> 그 둘이 따로 노는 바람에, 본문이 사실상 빈 페이지가 메타데이터 덕에 '충분' 판정을 받고 폴백을 건너뛴 적이 있습니다.
> 그래서 잘린 설명 속 날짜를 마감일로 잘못 읽었습니다. **이 규칙이 예언한 그대로입니다.**
> 이 저장소를 만들면서도 같은 실수를 한 번 더 냈습니다 → [수집가](agents/01-collector.md)

### 그럼 언제 합쳐도 되나

**에이전트를 나눈다는 건 개념을 나눈다는 뜻이지 호출을 나눈다는 뜻이 아닙니다.**

규칙1·2가 요구하는 분리는 두 가지뿐입니다 — **목표가 상충할 때**와 **쓴 것을 검수할 때**.
그 둘이 아니면 한 호출로 합쳐도 됩니다.

오렌지레터는 **분류가와 초안가를 한 번의 호출로 돌립니다.** 분류와 작성은 상충하지도 않고 검수 관계도 아니기 때문입니다.
아홉 명을 아홉 번의 호출로 읽으면 비용만 늘고 얻는 게 없습니다.

거꾸로, **합치면 안 되는 자리는 합치는 게 싸 보여도 합치지 마세요.** 수집가와 문지기가 그렇습니다.

## 규칙 2. 쓴 놈이 검수하지 않는다

**초안가**와 **사실 검증관·비평가**는 별도 호출입니다. 같은 대화에 이어서 "방금 쓴 거 검토해봐"라고 물으면
LLM은 자기 문장을 방어합니다. 통과율이 비정상적으로 높아지고, 검수 단계가 사실상 장식이 됩니다.

- 검수자에게는 **초안이 어떻게 나왔는지 알려주지 않습니다.** 원문과 초안만 줍니다.
- 검수자는 **고치지 않습니다.** 지적하고 대안을 낼 뿐, 반영 여부는 사람이 정합니다.
- 검수 관점이 여러 개면 검수자도 여러 명입니다. 사실 여부(검증관)와 문체·규칙(비평가)은 다른 눈입니다.

## 규칙 3. 매번 도는 것과 가끔 도는 것을 분리한다

같은 "개선"이라도 주기가 다르면 다른 에이전트입니다.

| | 재시도 담당 | 정비공 |
|---|---|---|
| 묻는 것 | 기다리면 되는가 | 코드를 고쳐야 하는가 |
| 하는 일 | 시간을 두고 다시 시도 | 원인을 진단하고 패치를 제안 |
| 주기 | 1시간마다 | 주 1회 |
| 코드 변경 | 없음 | 있음 (사람 승인 후) |

이걸 한 명에게 맡기면 매시간 코드를 고치려 들거나, 반대로 5분이면 회복될 일을 일주일 기다립니다.

문체 학습도 마찬가지입니다. 건별로 돌릴 필요가 없습니다.
오렌지레터는 **분기에 한 번** 학습해서 뽑은 규칙 파일을 계속 쓰고 있습니다 ([부록](appendix/voice-training.md)).

## 규칙 4. 결론 내는 에이전트와 선택지 내는 에이전트를 구분한다

**비평가**는 결론을 내지 않습니다. A안과 B안을 나란히 내고 사람이 고릅니다.
**정비공**도 그렇습니다. 패치를 짜서 PR로 올릴 뿐 스스로 반영하지 않습니다.

더 중요한 건 **아예 에이전트를 두지 않은 자리**입니다. 오렌지레터에서 여는 글은 사람이 통째로 씁니다.
이 구성에 그 자리가 없는 건 빠뜨린 게 아니라 두지 않기로 한 것입니다 — 여는 글은 조직의 목소리라서,
여기까지 자동화하면 매주 비슷한 글이 나오고 읽는 사람이 먼저 알아챕니다.

자동화하지 않기로 한 자리를 명시적으로 비워두는 것도 설계입니다.
어느 자리를 남길지는 조직마다 다릅니다. 다만 **"어디를 남겼는지 스스로 말할 수 있어야"** 합니다.

---

## 늘리는 순서

### 1단계 — 최소 구성 3명

```
문지기 → 초안가 → 사실 검증관
```

이 셋이 "AI가 지어내지 않고 우리 말투로 쓴다"를 만듭니다. 나머지는 다 편의 기능입니다.
여기서부터 시작하세요. 처음부터 아홉 명을 만들면 어느 단계가 문제인지 영영 모릅니다.

### 2단계 — 원자료가 지저분해지면

```
+ 수집가    원자료 형태가 여러 가지가 될 때
+ 재시도 담당    실패가 일시적인지 영구적인지 갈리기 시작할 때
```

### 3단계 — 양이 늘면

```
+ 분류가    한 번에 처리할 항목이 수십 건이 될 때
+ 비평가    초안을 매번 손으로 고치고 있다는 걸 깨달았을 때
```

### 4단계 — 매주 반복이 되면

```
+ 발행 담당    채널이 둘 이상이 될 때
+ 정비공    같은 실패가 반복되는데 매번 손으로 고치고 있을 때
+ 오케스트레이터    누구를 불러야 할지 헷갈리기 시작할 때
```

---

## 갈아끼울 자리 셋

| 축 | 무엇을 바꾸나 | 손대야 할 파일 |
|---|---|---|
| **원자료** | 웹페이지 → 보고서·설문·회의록 | `01-collector` · `02-gatekeeper` |
| **문체** | 조직의 말투와 형식 규칙 | `04-drafter` + [부록](appendix/voice-training.md) |
| **채널** | 이메일·카드뉴스 → 블로그·카카오·사내 공지 | `07-publisher` |

`03` `05` `06` `08` `09`는 대체로 그대로 씁니다. 여기까지 손대고 있다면 설계를 다시 보는 게 낫습니다.

---

## 자주 하는 실수

**프롬프트를 길게 써서 해결하려 한다.**
"지어내지 마"라고 백 번 써도 빈 입력이 들어가면 지어냅니다. 입력을 막아야 합니다.

**검수를 같은 대화에 이어 붙인다.**
규칙 2를 보세요. 별도 호출이 아니면 검수가 아닙니다.

**실패를 전부 살리려 한다.**
로그인이 필요한 페이지, 순수 자바스크립트 앱은 구조적으로 못 읽습니다.
목표는 "성공률 100%"가 아니라 **"못 읽는 걸 깔끔하게 못 읽는다고 분류하는 것"**입니다.
억지로 통과시키면 규칙 1의 사고가 납니다.

**게이트 기준을 낮춰 성공률을 올린다.**
숫자는 올라가고 품질은 내려갑니다. 게이트를 낮추기 전에 수집 단계를 고치세요.

**확신도를 검토 필터로 쓰면서 그 필터의 성능을 재지 않는다.**
"확신도 낮은 것만 사람이 보면 된다"는 처음엔 맞고 나중엔 틀립니다.
오렌지레터는 이 문장을 넉 달 동안 문서에 걸어뒀는데, 재보니 낮음 구간이 **오류의 39%밖에 못 잡고 있었습니다.**
품질이 나빠져서가 아니라 좋아지면서 생긴 일입니다 → [분류가](agents/03-classifier.md)

**조용한 자동화를 만든다.**
정비공은 고칠 게 없는 주에도 "없음"을 보고합니다.
아무 소리도 안 나는 자동화는 **잘 돌아가는 것과 죽은 것을 구분할 수 없습니다.**
